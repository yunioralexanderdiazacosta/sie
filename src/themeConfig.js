import {createMuiTheme} from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
      light: '#1c5375',
      main: '#183E56',
      dark: '#133647',
    },
    secondary: {
      light: '#d68733',
      main: '#CC7C26',
      dark: '#bf7322',
    },
  },
});

export default theme;
